name: Shell Commands
on: [push]
jobs:
 run-shell-commands:
  runs-on: ubuntu-latest
  steps:
   - name: Echo a String
     run: echo "Hello World"
   - name: Multiline
     run: |
      node -v
      npm -v
